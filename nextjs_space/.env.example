# Database connection string (only needed when CONTACT_DB_ENABLED=true)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Toggle to allow the contact API to persist records via Prisma.
# Leave as 'false' while testing on Vercel without a database.
CONTACT_DB_ENABLED=false
